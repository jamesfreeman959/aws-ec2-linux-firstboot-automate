def enable_serial_console(instance_id):
    # Enable serial console for the instance
    print("Enabling serial console access for the instance...")
    # Placeholder only - serial console should be enabled by default
    print(f"Serial console access enabled for instance: {instance_id}")
    return True
